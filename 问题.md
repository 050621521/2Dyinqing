Actor 蓝图的定位是：给某一类 Actor 写自己的行为逻辑，运行时每个绑定了这个 .bp 的 Actor 会有一份独立执行实例。和“关卡蓝图”不同，Actor 蓝图里主要用 Self.* 节点操作自己，比如“获取自身位置”“设置自身位置”“设置精灵颜色”。
目前可用流程是：
在底部“内容浏览器”空白处右键，点“新建蓝图类”。
双击生成的 .bp 文件打开蓝图编辑器。
在蓝图里添加事件节点，比如“开始运行”“按键按下”“每帧”。
用 Self.* 节点写行为，例如：“按键按下” → “设置自身位置”
“每帧” → “获取自身位置” → 数学加减 → “设置自身位置”
“开始运行” → “设置精灵颜色 / 设置激活”

点击顶部运行按钮，运行时会为绑定了这个蓝图类的 Actor 创建 ActorBPRuntime，执行这个 Actor 自己的蓝图逻辑。
但当前项目有一个关键缺口：界面里还没有完整的“把某个 .bp 蓝图类绑定到某个 Actor”的入口。代码里 Actor 有 bpClass 字段，运行时也会加载非 builtin/ 的蓝图类，但细节面板目前只支持图片拖拽到精灵，不支持拖 .bp 到 Actor；也没有下拉选择蓝图类的控件。所以现在最稳的实际结论是：Actor 蓝图编辑和运行框架已经有了，但“创建后绑定到 Actor”的编辑器流程还没打通。
临时验证可以手改 .level 里的 Actor：
“bpClass”: “Blueprints/你的蓝图名.bp”
然后重新打开关卡运行。正式使用前建议先补两个功能：细节面板增加”蓝图类”选择/拖拽绑定，以及 .bp 的保存/脏标记提示。当前 .bp 保存也不够稳，编辑完不要只关主窗口，最好先关闭当前蓝图 Tab 触发保存。

---

## 已核实的代码级 Bug（补充）

### Bug A — 「打印字符串」节点在 Actor 蓝图中完全无效
ActorBPRuntime.cpp:66-68 里 Action.Print 只写了 `return “exec_out”`，没有任何输出。
对比关卡蓝图（BPRuntime.cpp:125-127）会把文本追加到 `m_printLog` 并显示在游戏视口。
Actor 蓝图里的「打印字符串」节点现在是个空操作，调试时看不到任何输出。

### Bug B — 「设置精灵可见」和「设置激活」行为完全相同
ActorBPRuntime.cpp:117-121，Self.Sprite.SetVisible 实现是 `self->active = ...`，
与 Self.SetActive（line 87-91）完全一致。ActorData 没有独立的精灵可见字段，
导致两个节点做同一件事——把整个 Actor 激活/停用，而非只控制精灵显示。

### Bug C — UI 按钮/下拉事件在 Actor 蓝图中触发不了
ActorBPRuntime.cpp:256-262，triggerButtonClick 用 `refUi == instanceId` 匹配，
但 UI.Ref 节点输出的是 `uiName::widgetName` 格式，refUi 实际上是 uiName。
关卡蓝图（BPRuntime.cpp:366）已经加了 `|| refUi == uiName` 兜底，Actor 蓝图漏掉了。
结果：Actor 蓝图里从「UI引用」连出的「按钮点击时」和「下拉选项改变时」永远不会触发。

### Bug D — Actor 蓝图修改后关闭窗口不提示保存
BlueprintEditor 有 bpClassModified 信号（BlueprintEditor.h:27），但 EditorWindow.cpp:191
只连接了 documentModified。closeEvent（line 840-858）也只检查关卡文档脏状态，
然后直接 qDeleteAll(m_openBpClasses)。修改 Actor 蓝图后直接关窗口，改动静默丢失。

### Bug E — 按钮/下拉事件执行链缺少循环保护
ActorBPRuntime::triggerButtonClick 和 triggerDropdownChanged 调用 executeChain 时
没有传 visited 集合（对比 triggerEvent 传了 &visited）。
如果蓝图节点连线构成环路，会无限递归直到栈溢出。关卡蓝图的同名方法有 visited 保护。