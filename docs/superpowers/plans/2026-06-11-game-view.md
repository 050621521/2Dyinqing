# 游戏视图（Game View）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增游戏视图 Tab，以主摄像机视角渲染场景，运行时自动跳转，黑边适配宽高比。

**Architecture:** 新建 `GameViewport` 纯渲染 Widget，由 EditorWindow 包装进工具栏+Widget 的页面，嵌入 DocTabBar 作为固定不可关闭 Tab。摄像机数据模型从 aspect ratio 改为分辨率，新增 `cameraIsMain` 标记。

**Tech Stack:** C++17, Qt6 Widgets, CMake

---

## 文件清单

| 操作 | 文件 |
|------|------|
| 创建 | `launcher/src/editor/GameViewport.h` |
| 创建 | `launcher/src/editor/GameViewport.cpp` |
| 修改 | `launcher/src/models/LevelDocument.h` |
| 修改 | `launcher/src/models/LevelDocument.cpp` |
| 修改 | `launcher/src/editor/Viewport2D.cpp` |
| 修改 | `launcher/src/editor/DetailsPanel.h` |
| 修改 | `launcher/src/editor/DetailsPanel.cpp` |
| 修改 | `launcher/src/editor/ContentBrowser.cpp` |
| 修改 | `launcher/src/editor/DocTabBar.h` |
| 修改 | `launcher/src/editor/DocTabBar.cpp` |
| 修改 | `launcher/src/editor/EditorWindow.h` |
| 修改 | `launcher/src/editor/EditorWindow.cpp` |
| 修改 | `launcher/CMakeLists.txt` |

---

## Task 1: CMakeLists.txt — 注册 GameViewport 源文件

**Files:**
- Modify: `launcher/CMakeLists.txt`

- [ ] **Step 1: 在 SOURCES 列表中新增 GameViewport.cpp**

在 `src/editor/DocTabBar.cpp` 这一行后面加一行：

```cmake
    src/editor/GameViewport.cpp
```

- [ ] **Step 2: 在 HEADERS 列表中新增 GameViewport.h**

找到 HEADERS 列表，在 `src/editor/DocTabBar.h` 后面加一行：

```cmake
    src/editor/GameViewport.h
```

- [ ] **Step 3: 编译验证（GameViewport 文件还未创建，会报错 — 正常，继续下一 Task）**

```bash
cd /Users/kwy/Documents/2Dyinqing/launcher/build && cmake --build . -j$(sysctl -n hw.logicalcpu) 2>&1 | tail -5
```

---

## Task 2: 数据模型 — cameraAspect → cameraResW/H，新增 cameraIsMain

**Files:**
- Modify: `launcher/src/models/LevelDocument.h`
- Modify: `launcher/src/models/LevelDocument.cpp`
- Modify: `launcher/src/editor/Viewport2D.cpp`

- [ ] **Step 1: LevelDocument.h — 替换 cameraAspect，新增三个字段**

在 `LevelDocument.h` 的 `ActorData` 结构体中，找到：
```cpp
    float   cameraAspect       = 1.7778f;
```
替换为：
```cpp
    bool    cameraIsMain       = false;
    int     cameraResW         = 1920;
    int     cameraResH         = 1080;
```

- [ ] **Step 2: LevelDocument.cpp — toJson() 更新**

找到：
```cpp
    obj["cameraAspect"]       = cameraAspect;
```
替换为：
```cpp
    obj["cameraIsMain"]       = cameraIsMain;
    obj["cameraResW"]         = cameraResW;
    obj["cameraResH"]         = cameraResH;
```

- [ ] **Step 3: LevelDocument.cpp — fromJson() 更新**

找到：
```cpp
    a.cameraAspect       = (float)obj["cameraAspect"].toDouble(1.7778);
```
替换为：
```cpp
    a.cameraIsMain = obj["cameraIsMain"].toBool(false);
    a.cameraResW   = obj["cameraResW"].toInt(1920);
    a.cameraResH   = obj["cameraResH"].toInt(1080);
```

- [ ] **Step 4: Viewport2D.cpp — 摄像机视锥宽度改用分辨率推导**

找到（约第 216 行）：
```cpp
            const float halfW = halfH * a.cameraAspect;
```
替换为：
```cpp
            const float halfW = halfH * (a.cameraResH > 0 ? (float)a.cameraResW / a.cameraResH : 1.7778f);
```

- [ ] **Step 5: 编译验证（此时 DetailsPanel 仍引用 m_cameraAspectSpin，会报错 — 正常）**

```bash
cd /Users/kwy/Documents/2Dyinqing/launcher/build && cmake --build . -j$(sysctl -n hw.logicalcpu) 2>&1 | grep error | head -10
```

- [ ] **Step 6: Commit**

```bash
git add launcher/src/models/LevelDocument.h launcher/src/models/LevelDocument.cpp launcher/src/editor/Viewport2D.cpp launcher/CMakeLists.txt
git commit -m "feat: 摄像机数据模型 — 新增 cameraIsMain/cameraResW/H，移除 cameraAspect"
```

---

## Task 3: DetailsPanel — 摄像机组件 UI 更新

**Files:**
- Modify: `launcher/src/editor/DetailsPanel.h`
- Modify: `launcher/src/editor/DetailsPanel.cpp`

- [ ] **Step 1: DetailsPanel.h — 替换 m_cameraAspectSpin，新增三个成员**

找到：
```cpp
    QDoubleSpinBox* m_cameraAspectSpin   = nullptr;
```
替换为：
```cpp
    QCheckBox*      m_cameraIsMainCheck  = nullptr;
    QSpinBox*       m_cameraResWSpin     = nullptr;
    QSpinBox*       m_cameraResHSpin     = nullptr;
```

- [ ] **Step 2: DetailsPanel.cpp — showActor() 更新 QSignalBlocker 列表**

找到：
```cpp
               b16(m_cameraOrthoCheck), b17(m_cameraSizeSpin), b18(m_cameraAspectSpin),
```
替换为：
```cpp
               b16(m_cameraOrthoCheck), b17(m_cameraSizeSpin),
               b18(*m_cameraIsMainCheck), b18b(*m_cameraResWSpin), b18c(*m_cameraResHSpin),
```

- [ ] **Step 3: DetailsPanel.cpp — showActor() 更新摄像机字段赋值**

找到：
```cpp
    // 摄像机组件字段
    m_cameraOrthoCheck->setChecked(actor.cameraOrthographic);
    m_cameraSizeSpin->setValue(actor.cameraSize);
    m_cameraAspectSpin->setValue(actor.cameraAspect);
    m_cameraBgBtn->setStyleSheet(
        QString("background:%1; border:1px solid #555;")
        .arg(actor.cameraBackground.name(QColor::HexArgb)));
```
替换为：
```cpp
    // 摄像机组件字段
    m_cameraIsMainCheck->setChecked(actor.cameraIsMain);
    m_cameraOrthoCheck->setChecked(actor.cameraOrthographic);
    m_cameraSizeSpin->setValue(actor.cameraSize);
    m_cameraResWSpin->setValue(actor.cameraResW);
    m_cameraResHSpin->setValue(actor.cameraResH);
    m_cameraBgBtn->setStyleSheet(
        QString("background:%1; border:1px solid #555;")
        .arg(actor.cameraBackground.name(QColor::HexArgb)));
```

- [ ] **Step 4: DetailsPanel.cpp — buildCameraComponent() 完整替换**

找到整个 `buildCameraComponent` 函数，替换为：

```cpp
void DetailsPanel::buildCameraComponent(QVBoxLayout* root) {
    QVBoxLayout* bl = nullptr;
    auto [box_sb, content_sb] = makeSectionBox(this, &bl, "摄像机组件");
    QWidget* box = box_sb; QWidget* content = content_sb;

    auto* grid = new QGridLayout(content);
    grid->setSpacing(4);
    grid->setContentsMargins(8, 4, 8, 10);
    grid->setColumnMinimumWidth(0, 60);

    auto mkDSpin = [&](double lo, double hi, double val, double step = 0.1) -> QDoubleSpinBox* {
        auto* s = new QDoubleSpinBox(content);
        s->setRange(lo, hi); s->setValue(val); s->setSingleStep(step);
        s->setDecimals(3); s->setObjectName("detailSpin");
        connect(s, QOverload<double>::of(&QDoubleSpinBox::valueChanged),
                this, &DetailsPanel::onAnyFieldChanged);
        return s;
    };

    // 行0：主摄像机
    grid->addWidget(new QLabel("主摄像机", content), 0, 0);
    m_cameraIsMainCheck = new QCheckBox(content);
    m_cameraIsMainCheck->setObjectName("detailCheck");
    connect(m_cameraIsMainCheck, &QCheckBox::toggled, this, &DetailsPanel::onAnyFieldChanged);
    grid->addWidget(m_cameraIsMainCheck, 0, 1, 1, 2);

    // 行1：投影模式
    grid->addWidget(new QLabel("投影模式", content), 1, 0);
    m_cameraOrthoCheck = new QCheckBox("正交", content);
    m_cameraOrthoCheck->setObjectName("detailCheck");
    m_cameraOrthoCheck->setChecked(true);
    connect(m_cameraOrthoCheck, &QCheckBox::toggled, this, &DetailsPanel::onAnyFieldChanged);
    grid->addWidget(m_cameraOrthoCheck, 1, 1, 1, 2);

    // 行2：大小
    grid->addWidget(new QLabel("大小", content), 2, 0);
    m_cameraSizeSpin = mkDSpin(0.1, 9999, 5.0, 0.5);
    grid->addWidget(m_cameraSizeSpin, 2, 1, 1, 2);

    // 行3：分辨率
    grid->addWidget(new QLabel("分辨率", content), 3, 0);
    auto* resRow = new QHBoxLayout;
    m_cameraResWSpin = new QSpinBox(content);
    m_cameraResWSpin->setObjectName("detailSpin");
    m_cameraResWSpin->setRange(1, 7680);
    m_cameraResWSpin->setValue(1920);
    connect(m_cameraResWSpin, QOverload<int>::of(&QSpinBox::valueChanged),
            this, &DetailsPanel::onAnyFieldChanged);
    auto* resX = new QLabel("×", content);
    resX->setObjectName("detailSmallLabel");
    m_cameraResHSpin = new QSpinBox(content);
    m_cameraResHSpin->setObjectName("detailSpin");
    m_cameraResHSpin->setRange(1, 4320);
    m_cameraResHSpin->setValue(1080);
    connect(m_cameraResHSpin, QOverload<int>::of(&QSpinBox::valueChanged),
            this, &DetailsPanel::onAnyFieldChanged);
    resRow->addWidget(m_cameraResWSpin, 1);
    resRow->addWidget(resX);
    resRow->addWidget(m_cameraResHSpin, 1);
    grid->addLayout(resRow, 3, 1, 1, 2);

    // 行4：背景色
    grid->addWidget(new QLabel("背景色", content), 4, 0);
    m_cameraBgBtn = new QPushButton(content);
    m_cameraBgBtn->setObjectName("spriteColorBtn");
    m_cameraBgBtn->setFixedWidth(70);
    m_cameraBgBtn->setFixedHeight(22);
    m_cameraBgBtn->setStyleSheet("background:#ff1e1e1e; border:1px solid #555;");
    grid->addWidget(m_cameraBgBtn, 4, 1, 1, 2);

    connect(m_cameraBgBtn, &QPushButton::clicked, this, [this]() {
        QColor c = QColorDialog::getColor(m_currentActor.cameraBackground, this,
            "摄像机背景色", QColorDialog::ShowAlphaChannel);
        if (!c.isValid()) return;
        m_currentActor.cameraBackground = c;
        m_cameraBgBtn->setStyleSheet(
            QString("background:%1; border:1px solid #555;").arg(c.name(QColor::HexArgb)));
        emit actorModified(m_currentActor);
    });

    m_cameraBox = box;
    root->addWidget(box);
    box->hide();
}
```

- [ ] **Step 5: DetailsPanel.cpp — onAnyFieldChanged() 更新摄像机字段读取**

找到：
```cpp
    if (m_cameraBox && m_cameraBox->isVisible()) {
        a.cameraOrthographic = m_cameraOrthoCheck->isChecked();
        a.cameraSize         = (float)m_cameraSizeSpin->value();
        a.cameraAspect       = (float)m_cameraAspectSpin->value();
        a.cameraBackground   = m_currentActor.cameraBackground;
    }
```
替换为：
```cpp
    if (m_cameraBox && m_cameraBox->isVisible()) {
        a.cameraIsMain       = m_cameraIsMainCheck->isChecked();
        a.cameraOrthographic = m_cameraOrthoCheck->isChecked();
        a.cameraSize         = (float)m_cameraSizeSpin->value();
        a.cameraResW         = m_cameraResWSpin->value();
        a.cameraResH         = m_cameraResHSpin->value();
        a.cameraBackground   = m_currentActor.cameraBackground;
    }
```

- [ ] **Step 6: 编译验证**

```bash
pkill -x launcher 2>/dev/null; sleep 0.3; cd /Users/kwy/Documents/2Dyinqing/launcher/build && cmake --build . -j$(sysctl -n hw.logicalcpu) 2>&1 | tail -5
```

Expected: 编译成功（此时 GameViewport 文件尚未创建，CMake 会报 missing file，但 DetailsPanel 本身应无错误）

- [ ] **Step 7: Commit**

```bash
git add launcher/src/editor/DetailsPanel.h launcher/src/editor/DetailsPanel.cpp
git commit -m "feat: 摄像机组件 UI — 新增主摄像机开关，分辨率替换宽高比"
```

---

## Task 4: ContentBrowser — 新建关卡自动添加默认摄像机

**Files:**
- Modify: `launcher/src/editor/ContentBrowser.cpp`

- [ ] **Step 1: 在 ContentBrowser.cpp 顶部添加 QUuid 头文件**

找到现有 include 列表末尾（`#include <QFileDialog>` 后），添加：
```cpp
#include <QUuid>
```

- [ ] **Step 2: 替换 createLevelFile() 函数**

找到整个 `createLevelFile` 函数：
```cpp
bool ContentBrowser::createLevelFile(const QString& filePath) {
    QJsonObject obj;
    obj["name"]    = QFileInfo(filePath).baseName();
    obj["version"] = "0.1";
    obj["objects"] = QJsonArray();

    QFile f(filePath);
    if (!f.open(QIODevice::WriteOnly)) return false;
    f.write(QJsonDocument(obj).toJson(QJsonDocument::Indented));
    return true;
}
```
替换为：
```cpp
bool ContentBrowser::createLevelFile(const QString& filePath) {
    QJsonObject pos; pos["x"] = 0.0; pos["y"] = 0.0;
    QJsonObject scl; scl["x"] = 1.0; scl["y"] = 1.0;
    QJsonArray comps; comps.append("变换"); comps.append("摄像机组件");

    QJsonObject cam;
    cam["id"]                 = QUuid::createUuid().toString(QUuid::WithoutBraces);
    cam["name"]               = "主摄像机";
    cam["type"]               = "Camera";
    cam["position"]           = pos;
    cam["rotation"]           = 0.0;
    cam["scale"]              = scl;
    cam["active"]             = true;
    cam["static"]             = false;
    cam["tag"]                = "未标记";
    cam["layer"]              = "默认";
    cam["components"]         = comps;
    cam["spritePath"]         = "";
    cam["spriteColor"]        = "#ffffffff";
    cam["sortingLayer"]       = "默认";
    cam["orderInLayer"]       = 0;
    cam["flipX"]              = false;
    cam["flipY"]              = false;
    cam["drawMode"]           = "简单";
    cam["cameraSize"]         = 5.0;
    cam["cameraIsMain"]       = true;
    cam["cameraResW"]         = 1920;
    cam["cameraResH"]         = 1080;
    cam["cameraOrthographic"] = true;
    cam["cameraBackground"]   = "#ff1e1e1e";
    cam["followTarget"]       = "";
    cam["followLerpSpeed"]    = 5.0;
    cam["followOffsetX"]      = 0.0;
    cam["followOffsetY"]      = 0.0;
    cam["confinerEnabled"]    = false;
    cam["confinerMinX"]       = -500.0;
    cam["confinerMaxX"]       =  500.0;
    cam["confinerMinY"]       = -500.0;
    cam["confinerMaxY"]       =  500.0;

    QJsonArray objects;
    objects.append(cam);

    QJsonObject root;
    root["name"]    = QFileInfo(filePath).baseName();
    root["version"] = "0.1";
    root["objects"] = objects;

    QFile f(filePath);
    if (!f.open(QIODevice::WriteOnly)) return false;
    f.write(QJsonDocument(root).toJson(QJsonDocument::Indented));
    return true;
}
```

- [ ] **Step 3: Commit**

```bash
git add launcher/src/editor/ContentBrowser.cpp
git commit -m "feat: 新建关卡自动添加默认主摄像机 Actor"
```

---

## Task 5: DocTabBar — 新增 kGameViewTabData 常量

**Files:**
- Modify: `launcher/src/editor/DocTabBar.h`
- Modify: `launcher/src/editor/DocTabBar.cpp`

- [ ] **Step 1: DocTabBar.h — 新增常量声明**

找到：
```cpp
    static const QString kBlueprintTabData;  // 值为 "::blueprint::"
```
替换为：
```cpp
    static const QString kBlueprintTabData;  // 值为 "::blueprint::"
    static const QString kGameViewTabData;   // 值为 "::gameview::"
```

- [ ] **Step 2: DocTabBar.cpp — 新增常量定义**

找到：
```cpp
const QString DocTabBar::kBlueprintTabData = QStringLiteral("::blueprint::");
```
替换为：
```cpp
const QString DocTabBar::kBlueprintTabData = QStringLiteral("::blueprint::");
const QString DocTabBar::kGameViewTabData  = QStringLiteral("::gameview::");
```

- [ ] **Step 3: Commit**

```bash
git add launcher/src/editor/DocTabBar.h launcher/src/editor/DocTabBar.cpp
git commit -m "feat: DocTabBar 新增 kGameViewTabData 常量"
```

---

## Task 6: 创建 GameViewport Widget

**Files:**
- Create: `launcher/src/editor/GameViewport.h`
- Create: `launcher/src/editor/GameViewport.cpp`

- [ ] **Step 1: 创建 GameViewport.h**

```cpp
#pragma once
#include "models/LevelDocument.h"
#include <QWidget>
#include <QHash>
#include <QPixmap>

class GameViewport : public QWidget {
    Q_OBJECT
public:
    explicit GameViewport(QWidget* parent = nullptr);

    void loadLevel(LevelDocument* doc);
    void setRuntimeActors(const QList<ActorData>& actors);
    void setRuntimeMode(bool on);

protected:
    void paintEvent(QPaintEvent*) override;

private:
    QRectF  computeCameraRect(float aspect) const;
    QPointF cameraWorldToScreen(QPointF world, const QRectF& camRect,
                                const ActorData& cam) const;
    void    drawScene(QPainter& p, const QList<ActorData>& actors,
                      const ActorData& cam, const QRectF& camRect);

    LevelDocument*   m_doc         = nullptr;
    QList<ActorData> m_runtimeActors;
    bool             m_runtimeMode = false;
    mutable QHash<QString, QPixmap> m_pixmapCache;
};
```

- [ ] **Step 2: 创建 GameViewport.cpp**

```cpp
#include "GameViewport.h"
#include "models/ActorTypeUtils.h"
#include <QPainter>
#include <QPaintEvent>
#include <QFont>
#include <cmath>

GameViewport::GameViewport(QWidget* parent) : QWidget(parent) {
    setObjectName("gameViewport");
}

void GameViewport::loadLevel(LevelDocument* doc) {
    m_doc = doc;
    m_pixmapCache.clear();
    update();
}

void GameViewport::setRuntimeActors(const QList<ActorData>& actors) {
    m_runtimeActors = actors;
    update();
}

void GameViewport::setRuntimeMode(bool on) {
    m_runtimeMode = on;
    if (!on) m_runtimeActors.clear();
    update();
}

void GameViewport::paintEvent(QPaintEvent*) {
    QPainter p(this);
    p.setRenderHint(QPainter::Antialiasing);
    p.fillRect(rect(), Qt::black);

    const QList<ActorData>* actorsList = nullptr;
    if (m_runtimeMode)
        actorsList = &m_runtimeActors;
    else if (m_doc)
        actorsList = &m_doc->sortedActors();
    else
        return;

    const ActorData* cam = nullptr;
    for (const ActorData& a : *actorsList) {
        if (a.components.contains("摄像机组件") && a.cameraIsMain) {
            cam = &a;
            break;
        }
    }

    if (!cam) {
        p.setPen(QColor(180, 180, 180));
        p.setFont(QFont("PingFang SC", 14));
        p.drawText(rect(), Qt::AlignCenter, "场景中无主摄像机");
        return;
    }

    const float aspect = cam->cameraResH > 0
                         ? (float)cam->cameraResW / cam->cameraResH
                         : 1.7778f;
    const QRectF camRect = computeCameraRect(aspect);
    p.fillRect(camRect, cam->cameraBackground);
    drawScene(p, *actorsList, *cam, camRect);
}

QRectF GameViewport::computeCameraRect(float aspect) const {
    const float w = (float)width();
    const float h = (float)height();
    float camW, camH;
    if (h > 0.0f && w / h > aspect) {
        camH = h;
        camW = h * aspect;
    } else {
        camW = w;
        camH = (aspect > 0.0f) ? w / aspect : h;
    }
    return QRectF((w - camW) / 2.0f, (h - camH) / 2.0f, camW, camH);
}

QPointF GameViewport::cameraWorldToScreen(QPointF world, const QRectF& camRect,
                                           const ActorData& cam) const {
    const float halfH  = cam.cameraSize;
    const float aspect = cam.cameraResH > 0
                         ? (float)cam.cameraResW / cam.cameraResH
                         : 1.7778f;
    const float halfW  = halfH * aspect;
    const float scaleX = (float)camRect.width()  / (halfW * 2.0f);
    const float scaleY = (float)camRect.height() / (halfH * 2.0f);
    return QPointF(
        camRect.center().x() + (world.x() - cam.x) * scaleX,
        camRect.center().y() - (world.y() - cam.y) * scaleY
    );
}

void GameViewport::drawScene(QPainter& p, const QList<ActorData>& actors,
                              const ActorData& cam, const QRectF& camRect) {
    const float halfH  = cam.cameraSize;
    const float aspect = cam.cameraResH > 0
                         ? (float)cam.cameraResW / cam.cameraResH
                         : 1.7778f;
    const float halfW  = halfH * aspect;
    const float scaleX = (float)camRect.width()  / (halfW * 2.0f);
    const float scaleY = (float)camRect.height() / (halfH * 2.0f);
    const float scale  = qMin(scaleX, scaleY);

    p.setClipRect(camRect);

    for (const ActorData& a : actors) {
        if (a.type == "Camera") continue;

        QPointF pos = cameraWorldToScreen({a.x, a.y}, camRect, cam);
        const float szBase = qMax(24.0f, 40.0f * scale);
        const float szW    = szBase * qMax(0.05f, qAbs(a.scaleX));
        const float szH    = szBase * qMax(0.05f, qAbs(a.scaleY));
        QRectF aRect(pos.x() - szW / 2.0f, pos.y() - szH / 2.0f, szW, szH);

        p.save();
        if (a.rotation != 0.0f) {
            p.translate(pos.x(), pos.y());
            p.rotate(a.rotation);
            p.translate(-pos.x(), -pos.y());
        }

        bool drewPixmap = false;
        if (!a.spritePath.isEmpty()) {
            if (!m_pixmapCache.contains(a.spritePath))
                m_pixmapCache[a.spritePath] = QPixmap(a.spritePath);
            const QPixmap& px = m_pixmapCache[a.spritePath];
            if (!px.isNull()) {
                p.save();
                if (a.flipX || a.flipY) {
                    QTransform t;
                    t.translate(pos.x(), pos.y());
                    t.scale(a.flipX ? -1.0 : 1.0, a.flipY ? -1.0 : 1.0);
                    t.translate(-pos.x(), -pos.y());
                    p.setTransform(t, true);
                }
                p.setOpacity(a.spriteColor.alphaF());
                if (a.drawMode == "平铺")
                    p.drawTiledPixmap(aRect.toRect(), px);
                else
                    p.drawPixmap(aRect.toRect(), px);
                if (a.spriteColor.red() != 255 || a.spriteColor.green() != 255
                        || a.spriteColor.blue() != 255) {
                    p.setCompositionMode(QPainter::CompositionMode_SourceAtop);
                    QColor tint = a.spriteColor; tint.setAlpha(100);
                    p.fillRect(aRect, tint);
                    p.setCompositionMode(QPainter::CompositionMode_SourceOver);
                }
                p.setOpacity(1.0);
                p.restore();
                drewPixmap = true;
            }
        }

        if (!drewPixmap) {
            const QColor fill = actorTypeColor(a.type);
            p.setPen(QPen(fill.darker(160), 1.0));
            p.setBrush(fill);
            p.drawRect(aRect);
        }

        p.restore();
    }

    p.setClipping(false);
}
```

- [ ] **Step 3: 编译验证（GameViewport 自身应无错误）**

```bash
cd /Users/kwy/Documents/2Dyinqing/launcher/build && cmake --build . -j$(sysctl -n hw.logicalcpu) 2>&1 | grep -E "error:|GameViewport" | head -20
```

Expected: GameViewport 相关编译通过，可能仍有 EditorWindow 引用 GameViewport 的错误（下一 Task 处理）

- [ ] **Step 4: Commit**

```bash
git add launcher/src/editor/GameViewport.h launcher/src/editor/GameViewport.cpp
git commit -m "feat: 新建 GameViewport — 主摄像机视角渲染，黑边适配宽高比"
```

---

## Task 7: EditorWindow — 游戏视图 Tab 集成 + 主摄像机互斥

**Files:**
- Modify: `launcher/src/editor/EditorWindow.h`
- Modify: `launcher/src/editor/EditorWindow.cpp`

- [ ] **Step 1: EditorWindow.h — 新增前向声明、成员变量和私有方法**

在 `class Viewport2D;` 前向声明后面加一行：
```cpp
class GameViewport;
```

在 private 成员变量区，`Viewport2D* m_viewport = nullptr;` 那一行后面加：
```cpp
    GameViewport*    m_gameViewport   = nullptr;
    QWidget*         m_gameViewPage   = nullptr;
    QLabel*          m_gvCamNameLabel = nullptr;
    QLabel*          m_gvResLabel     = nullptr;
```

在 `QWidget* buildViewportToolBar(QWidget* parent);` 声明后面加：
```cpp
    QWidget* buildGameViewToolBar(QWidget* parent);
    void     updateGameViewToolbar(LevelDocument* doc);
```

- [ ] **Step 2: EditorWindow.cpp — 添加 GameViewport include**

在 `#include "Viewport2D.h"` 这一行后面加：
```cpp
#include "GameViewport.h"
```

- [ ] **Step 3: EditorWindow.cpp — setupCentralArea() 添加游戏视图页面和 Tab**

在 `setupCentralArea()` 末尾，`m_layoutManager = new LayoutManager(...)` 那一行的前面，添加：

```cpp
    // ── 游戏视图页面 ──────────────────────────────────────────────────────
    auto* gvWrap = new QWidget();
    gvWrap->setObjectName("gameViewWrap");
    auto* gvLay = new QVBoxLayout(gvWrap);
    gvLay->setContentsMargins(0, 0, 0, 0);
    gvLay->setSpacing(0);
    gvLay->addWidget(buildGameViewToolBar(gvWrap));
    m_gameViewport = new GameViewport(gvWrap);
    gvLay->addWidget(m_gameViewport, 1);
    m_gameViewPage = gvWrap;
    m_centralStack->addWidget(m_gameViewPage);  // index 2

    // 在 DocTabBar 末尾固定添加「游戏视图」Tab（不可关闭）
    {
        QSignalBlocker b(m_docTabBar);
        const int gvIdx = m_docTabBar->addTab("  游戏视图");
        m_docTabBar->setTabData(gvIdx, DocTabBar::kGameViewTabData);
        m_docTabBar->setTabButton(gvIdx, QTabBar::RightSide, nullptr);
        m_docTabBar->setTabButton(gvIdx, QTabBar::LeftSide,  nullptr);
    }
```

- [ ] **Step 4: EditorWindow.cpp — 新增 buildGameViewToolBar() 方法**

在 `buildViewportToolBar` 函数的结束大括号 `}` 后面，添加新函数：

```cpp
QWidget* EditorWindow::buildGameViewToolBar(QWidget* parent) {
    auto* bar = new QWidget(parent);
    bar->setObjectName("viewportToolBar");
    bar->setFixedHeight(28);
    auto* hl = new QHBoxLayout(bar);
    hl->setContentsMargins(6, 2, 6, 2);
    hl->setSpacing(6);

    m_gvCamNameLabel = new QLabel("摄像机：—", bar);
    m_gvCamNameLabel->setObjectName("vpZoomLabel");
    hl->addWidget(m_gvCamNameLabel);

    auto* sep1 = new QFrame(bar);
    sep1->setFrameShape(QFrame::VLine);
    sep1->setObjectName("vpSep");
    hl->addSpacing(4); hl->addWidget(sep1); hl->addSpacing(4);

    m_gvResLabel = new QLabel("—", bar);
    m_gvResLabel->setObjectName("vpZoomLabel");
    hl->addWidget(m_gvResLabel);

    hl->addStretch();

    auto* fsBtn = new QToolButton(bar);
    fsBtn->setText("⛶");
    fsBtn->setToolTip("最大化窗口");
    fsBtn->setObjectName("vpTBBtn");
    hl->addWidget(fsBtn);
    connect(fsBtn, &QToolButton::clicked, this, [this]() {
        isMaximized() ? showNormal() : showMaximized();
    });

    return bar;
}
```

- [ ] **Step 5: EditorWindow.cpp — 新增 updateGameViewToolbar() 方法**

在 `buildGameViewToolBar` 函数后面添加：

```cpp
void EditorWindow::updateGameViewToolbar(LevelDocument* doc) {
    if (!m_gvCamNameLabel || !m_gvResLabel) return;
    if (!doc) {
        m_gvCamNameLabel->setText("摄像机：—");
        m_gvResLabel->setText("—");
        return;
    }
    for (const ActorData& a : doc->actors()) {
        if (a.components.contains("摄像机组件") && a.cameraIsMain) {
            m_gvCamNameLabel->setText("摄像机：" + a.name);
            m_gvResLabel->setText(QString("%1×%2").arg(a.cameraResW).arg(a.cameraResH));
            return;
        }
    }
    m_gvCamNameLabel->setText("摄像机：无");
    m_gvResLabel->setText("—");
}
```

- [ ] **Step 6: EditorWindow.cpp — onTabChanged() 重构：path 提前，不中断游戏视图 Tab 时的 runtime**

找到 `onTabChanged` 函数开头：
```cpp
void EditorWindow::onTabChanged(int index) {
    if (m_runtime) stopRuntime();
    if (!m_sceneOutliner || !m_detailsPanel) return;

    for (auto& conn : m_tabConnections) disconnect(conn);
    m_tabConnections.clear();

    const QString path = m_docTabBar->tabData(index).toString();
```
替换为：
```cpp
void EditorWindow::onTabChanged(int index) {
    const QString path = m_docTabBar->tabData(index).toString();
    if (m_runtime && path != DocTabBar::kGameViewTabData)
        stopRuntime();
    if (!m_sceneOutliner || !m_detailsPanel) return;

    for (auto& conn : m_tabConnections) disconnect(conn);
    m_tabConnections.clear();
```

- [ ] **Step 7: EditorWindow.cpp — onTabChanged() 添加游戏视图 Tab 分支**

在蓝图 Tab 分支（`if (path == DocTabBar::kBlueprintTabData)`）的 `return;` 后面，紧接着添加：

```cpp
    // 游戏视图 Tab
    if (path == DocTabBar::kGameViewTabData) {
        if (m_centralStack) m_centralStack->setCurrentWidget(m_gameViewPage);
        LevelDocument* doc = m_openLevels.value(m_activeLevelPath, nullptr);
        if (m_gameViewport) {
            m_gameViewport->loadLevel(doc);
            updateGameViewToolbar(doc);
        }
        return;
    }
```

- [ ] **Step 8: EditorWindow.cpp — onTabClosed() 拦截游戏视图关闭请求**

在 `onTabClosed` 开头的蓝图拦截前面加一行：

找到：
```cpp
    if (path == DocTabBar::kBlueprintTabData) {
```
在其前面添加：
```cpp
    if (path == DocTabBar::kGameViewTabData) return;
```

- [ ] **Step 9: EditorWindow.cpp — actorModified 连接中添加主摄像机互斥 + 游戏视图刷新**

在 `onTabChanged` 里找到：
```cpp
    m_tabConnections << connect(m_detailsPanel, &DetailsPanel::actorModified,
                                this, [this, doc](const ActorData& a) {
        doc->updateActor(a);
        updateTabTitle(m_docTabBar->currentIndex());
        updateSaveLabel();
        if (m_viewport) m_viewport->update();
        m_sceneOutliner->loadLevel(doc);
    });
```
替换为：
```cpp
    m_tabConnections << connect(m_detailsPanel, &DetailsPanel::actorModified,
                                this, [this, doc](const ActorData& a) {
        // 主摄像机互斥：取消同关卡其他摄像机的主摄像机标记
        if (a.components.contains("摄像机组件") && a.cameraIsMain) {
            for (const ActorData& other : doc->actors()) {
                if (other.id != a.id && other.components.contains("摄像机组件")
                        && other.cameraIsMain) {
                    ActorData updated = other;
                    updated.cameraIsMain = false;
                    doc->updateActor(updated);
                }
            }
        }
        doc->updateActor(a);
        updateTabTitle(m_docTabBar->currentIndex());
        updateSaveLabel();
        if (m_viewport) m_viewport->update();
        if (m_gameViewport) {
            m_gameViewport->update();
            updateGameViewToolbar(doc);
        }
        m_sceneOutliner->loadLevel(doc);
    });
```

- [ ] **Step 10: 编译并启动验证**

```bash
pkill -x launcher 2>/dev/null; sleep 0.3; cd /Users/kwy/Documents/2Dyinqing/launcher/build && cmake --build . -j$(sysctl -n hw.logicalcpu) && open launcher.app
```

验收：
- Tab 栏末尾出现「游戏视图」Tab，无关闭按钮
- 点击「游戏视图」Tab，视口区域切换为黑色画面（显示"场景中无主摄像机"或摄像机画面）
- 细节面板的摄像机组件区块显示「主摄像机」复选框和「分辨率 1920 × 1080」
- 新建关卡后，大纲中出现「主摄像机」Actor

- [ ] **Step 11: Commit**

```bash
git add launcher/src/editor/EditorWindow.h launcher/src/editor/EditorWindow.cpp
git commit -m "feat: EditorWindow 集成游戏视图 Tab，主摄像机互斥逻辑"
```

---

## Task 8: EditorWindow — 运行时联动

**Files:**
- Modify: `launcher/src/editor/EditorWindow.cpp`

- [ ] **Step 1: startRuntime() — 添加游戏视图 runtime 模式和自动跳转**

找到 `startRuntime()` 中：
```cpp
    m_viewport->setRuntimeMode(true, m_runtime->actors());
    m_runtime->triggerBeginPlay();
    m_viewport->updateRuntimeActors(m_runtime->actors());
    m_viewport->syncPrintLog(m_runtime->printLog());

    if (m_runBtn)  m_runBtn->setEnabled(false);
    if (m_stopBtn) m_stopBtn->setEnabled(true);
```
替换为：
```cpp
    m_viewport->setRuntimeMode(true, m_runtime->actors());
    m_runtime->triggerBeginPlay();
    m_viewport->updateRuntimeActors(m_runtime->actors());
    m_viewport->syncPrintLog(m_runtime->printLog());

    // 游戏视图运行时模式
    if (m_gameViewport) {
        m_gameViewport->setRuntimeMode(true);
        m_gameViewport->setRuntimeActors(m_runtime->actors());
    }
    // 自动跳转到游戏视图 Tab
    for (int i = 0; i < m_docTabBar->count(); ++i) {
        if (m_docTabBar->tabData(i).toString() == DocTabBar::kGameViewTabData) {
            m_docTabBar->setCurrentIndex(i);
            break;
        }
    }

    if (m_runBtn)  m_runBtn->setEnabled(false);
    if (m_stopBtn) m_stopBtn->setEnabled(true);
```

- [ ] **Step 2: startRuntime() 里的 stateChanged 连接 — 同步更新游戏视图**

找到：
```cpp
    connect(m_runtime, &BPRuntime::stateChanged, this, [this]() {
        if (!m_runtime || !m_viewport) return;
        m_viewport->updateRuntimeActors(m_runtime->actors());
        m_viewport->syncPrintLog(m_runtime->printLog());
    });
```
替换为：
```cpp
    connect(m_runtime, &BPRuntime::stateChanged, this, [this]() {
        if (!m_runtime || !m_viewport) return;
        m_viewport->updateRuntimeActors(m_runtime->actors());
        m_viewport->syncPrintLog(m_runtime->printLog());
        if (m_gameViewport)
            m_gameViewport->setRuntimeActors(m_runtime->actors());
    });
```

- [ ] **Step 3: stopRuntime() — 退出游戏视图 runtime 模式**

找到：
```cpp
    if (m_viewport) {
        m_viewport->setRuntimeMode(false);
        m_viewport->clearPrintLog();
    }
```
替换为：
```cpp
    if (m_viewport) {
        m_viewport->setRuntimeMode(false);
        m_viewport->clearPrintLog();
    }
    if (m_gameViewport)
        m_gameViewport->setRuntimeMode(false);
```

- [ ] **Step 4: 编译并完整验收**

```bash
pkill -x launcher 2>/dev/null; sleep 0.3; cd /Users/kwy/Documents/2Dyinqing/launcher/build && cmake --build . -j$(sysctl -n hw.logicalcpu) && open launcher.app
```

验收清单：
1. 新建关卡 → 大纲出现「主摄像机」，游戏视图显示摄像机背景色 + 工具栏显示「摄像机：主摄像机  1920×1080」
2. 场景放置 Sprite Actor → 游戏视图实时更新显示该 Actor
3. 细节面板勾选「主摄像机」→ 其他摄像机的「主摄像机」自动取消（互斥）
4. 摄像机移动位置 → 游戏视图画面随之平移
5. 修改摄像机大小（cameraSize）→ 游戏视图缩放
6. 调整窗口大小 → 黑边正确保持 1920:1080 比例
7. 点击 ▶ 运行 → 自动跳转到游戏视图 Tab，runtime actors 正确渲染
8. 点击 ⏹ 停止 → 游戏视图回到静态场景预览

- [ ] **Step 5: Commit**

```bash
git add launcher/src/editor/EditorWindow.cpp
git commit -m "feat: 游戏视图运行时联动 — 运行自动跳转，stateChanged 同步更新"
```
