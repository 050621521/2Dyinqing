请使用 Superpowers 的工作流程分析这个新需求。

现在不要直接修改代码，先做需求分析和实现方案。

需求名称：
右侧“大纲”和“细节”面板支持浮动、停靠和默认布局还原。

背景：
当前编辑器右侧有两个主要面板：

1. 大纲面板
2. 细节面板

截图中红框位置是右侧面板区域的顶部停靠位置 / 面板标题栏区域。

新增需求：
大纲面板和细节面板都需要支持类似编辑器面板系统的拖拽布局能力。

具体期望：

1. 面板可以拖拽成独立弹窗

* 用户可以拖拽“大纲”面板标题栏，使大纲面板脱离右侧区域，变成独立浮动弹窗。
* 用户可以拖拽“细节”面板标题栏，使细节面板脱离右侧区域，变成独立浮动弹窗。
* 浮动弹窗需要保留原面板内容和交互状态。

2. 面板可以重新嵌入右侧区域

* 当“大纲”或“细节”面板处于浮动弹窗状态时，用户可以将其拖拽到截图红框标记的右侧停靠区域。
* 拖到有效停靠区域后，面板应重新嵌入右侧布局中。
* 嵌入后应恢复为右侧面板的一部分，而不是创建新的重复面板。

3. 面板可以从右侧区域再次拖出

* 当“大纲”或“细节”面板已经嵌入右侧区域后，用户仍然可以拖拽它的标题栏。
* 如果用户将面板从右侧停靠区域拖出，它应该重新变成独立浮动弹窗。
* 这个过程不能丢失面板状态，也不能重复创建面板实例。

4. 增加默认布局还原按钮

* 需要增加一个“还原布局”按钮。
* 点击后，右侧面板恢复默认布局：

  * 大纲面板回到右侧上方区域
  * 细节面板回到右侧下方区域
* 如果大纲或细节当前是浮动弹窗，点击还原后应关闭浮动状态并回到默认停靠位置。
* 如果面板顺序、大小、停靠状态被用户改变，点击还原后都应恢复默认状态。

核心要求：

1. 大纲和细节面板都必须支持：

   * docked：嵌入右侧区域
   * floating：独立浮动弹窗
2. 拖拽切换时不要重复创建多个面板实例。
3. 拖拽切换时不要丢失面板内部状态。
4. 不要影响中央 2D 视口、顶部工具栏、底部日志栏等其他区域。
5. 如果项目已有弹窗/拖拽/停靠逻辑，优先复用现有系统，不要重新写一套重复逻辑。
6. 先分析当前代码结构，不要直接实现。

请重点分析：

1. 当前“大纲”和“细节”面板分别由哪些组件/文件实现。
2. 当前项目是否已有浮动窗口、拖拽面板、停靠区域或布局管理系统。
3. 右侧面板目前是固定布局，还是已经有可扩展的 Panel/Dock 系统。
4. 应该如何抽象面板状态：

   * docked
   * floating
   * hidden
5. “还原布局”应该放在哪里更合适。
6. 是否需要保存用户布局状态。
7. 是否需要提供默认布局配置。
8. 这个需求会影响哪些已有功能。

请先输出：

1. 你对需求的理解。
2. 涉及的代码模块和文件。
3. 推荐实现方案。
4. 最小修改路径。
5. 风险点。
6. 测试用例清单。

在我确认方案之前，不要修改代码。
