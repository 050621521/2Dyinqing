投影组件 (Projection/Camera Component)
这是定义“如何看”的组件，它控制渲染管线输出的画面特征。

作用： 设置投影模式。在2D中通常使用 正交投影 (Orthographic)。

关键点： 定义视口（Viewport）的大小、宽高比以及渲染深度（Clipping Planes），确保无论窗口分辨率如何变化，游戏画面都能按预期显示。

跟随控制组件 (Follow/Targeting Component)
这是实现游戏逻辑（如角色跟随）的核心。

作用： 逻辑上绑定一个目标对象（Target，如玩家角色）。

关键点： * 平滑跟随（Smoothing/Lerp）： 为了避免镜头跳动，通常使用线性插值算法实现平滑跟随。

偏移量（Offset）： 允许摄像机不直接以目标为中心，而是保持一定的视觉偏移。

边界与限制组件 (Confiner/Boundaries Component)
防止摄像机超出地图的可见范围。

作用： 定义一个矩形区域（Boundary Box），当摄像机到达边界时停止移动。

关键点： 防止玩家看到地图边缘之外的“虚空”区域。